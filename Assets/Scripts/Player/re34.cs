using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class re34 : MonoBehaviour
{
    public float speed;
    public int MaxHeal;
    private Rigidbody2D rigidbody2D;

    private void Awake()
    {
        rigidbody2D = GetComponent<Rigidbody2D>();
    }

    void Start()
    {
        
    }

     void Update()
     {
         Move();
     }

     void Move()
     {
         float har = Input.GetAxis("Horizontal");
         float ver = Input.GetAxis("Vertical");
         rigidbody2D.velocity = new Vector2(har, ver).normalized * speed;
     }
}
*/
